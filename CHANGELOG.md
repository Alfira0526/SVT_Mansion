# 변경 이력 (Changelog)

세 페르소나 진단(**A** 재미·공유 / **B** 기술·안정성 / **C** 비주얼)을 기반으로 게임을 개선한 기록입니다. 최신순.

## 19차 — 이웃 판타지 심화 (이웃 공개 리빌 · 내 사건 우선 · 이웃 궁합)
- **① 이웃 공개 리빌 팝업**: 이사하기 직후 이벤트 팝업들 **앞에**, *"두구두구… 당신의 이웃이 공개됩니다!"* 카드로 내 옆집·윗집·아랫집을 사진 모핑 + 관계·이름으로 극적으로 공개. (A의 감정 절정)
- **② 내 이웃 사건 우선**: "우리 아파트 이야기" → **"우리 동네 이야기"** 로 바꾸고, *내 이웃이 등장하는 사건*을 🏠 표시와 함께 맨 위로 정렬·강조.
- **③ 이웃 궁합 한 줄**: 내 이웃 조합에 맞는 따뜻한 코멘트를 히어로에 추가 (예: "부석순 보컬 사이에 낀 당신, 성대 배틀 관중석 당첨 🎤").
- 리빌 시퀀스(이웃 카드 → 이벤트) · 내 사건 정렬 · 궁합 분기 · 회귀(에러 0) 검증.

## 18차 — 북극성 재정립: "세븐틴이 내 이웃이라면" (나 중심 재설계)
- **"왜 하는지 모르겠다" 피드백 → 서비스 기초 의도 재점검** (5인 팀 토의). 핵심 판타지를 **A: "세븐틴이 내 이웃이라면"** 으로 확정 — 주인공은 나(캐럿), 재미의 핵심은 *"내 옆집·윗집·아랫집이 누구인가"*.
- **결과 히어로를 "🏠 나의 이웃"으로 교체**: 캐럿의 호수 + 왼쪽/오른쪽 옆집·윗집·아랫집을 사진·이름·성향과 함께 노출. 캐럿 미입주 시 "나를 배치하면 이웃이 생겨요" 안내.
- **경쟁·최적화 장치 제거** (코지 판타지와 충돌): 케미 지수 점수 · 최고기록 · 🎲 리롤 버튼 삭제. (17·16차에 얹었던 게임 점수 레이어 회수)
- **실시간 힌트 재초점**: "실시간 케미 N점" → **"내 이웃: 왼쪽 옆집 도겸 · 윗집 우지"** 처럼 이웃이 만들어지는 과정을 보여줌.
- **공유 문구 재초점**: "내 이웃은 도겸·승관·우지·원우! 🏠 세븐틴 아파트 사는 중". 상단 안내문·타이틀도 "나를 먼저 배치 → 내 이웃" 흐름으로.
- 이웃 산출(방향/건물 경계)·4분기 렌더·공유 문구·회귀(에러 0) 검증.

## 17차 — 실시간 케미 힌트 + 리롤/최고기록 리플레이 훅
- **배치 중 실시간 케미 힌트(`#liveHint`)**: 이사하기 전에도 **실시간 케미 지수 · 발동 예정 이벤트 수 · 코칭 팁**을 즉시 표시 → 배치 과정의 기대감/피드백(QA 지적) 해소. 붙여둘수록·유닛끼리 모을수록 점수가 오르는 게 눈에 보임.
- **최고 케미 지수 기록(리플레이 훅)**: 결과 판정에 **내 최고 점수** 표기, 갱신 시 "🎉 최고 기록 갱신!" 축하. `localStorage`에 누적 → 더 높은 점수 도전 루프.
- **🎲 다른 조합으로 다시 도전! 버튼**: 결과 카드 아래(캡처 영역 밖)에서 **무작위 재배치 + 즉시 결과 갱신**(시트 전송·스로틀 없음) → 원탭 재도전.
- 팀 백로그 2·3번 반영. 실시간 힌트 갱신·기록 저장/갱신·리롤 재생성·회귀(에러 0) 검증.

## 16차 — 아파트 유형 진단 + 케미 지수 (결과 판정 히어로)
- **"게임이 재미없다" 피드백 → 판정(verdict) 후크 추가** (5인 팀 회의: 팀장·QA·개발·기획·디자인). 결과 화면에 **아파트 유형 + 케미 지수(/100)** 를 최상단 히어로로 노출.
- **유형 8종** — 세븐틴 3유닛(힙합/보컬/퍼포) 인접 케미·캐럿 이웃·입주율·발생 이벤트를 종합해 판정. 예: 💎 인싸 캐럿 하우스 · 🎬 종합 예능 하우스 · 🎤 심야 사이퍼 하우스 · 🎧 복도 라이브홀 하우스 · 🕺 24시 연습실 하우스 · 💕 알콩달콩 케미 하우스 · 🌙 미니멀 고요 하우스.
- **케미 지수**: 이벤트 풍부함·이웃 상호작용·입주율·캐럿 이웃을 점수화(55~99), MBTI형 결과처럼 후하게 설계 → 캡처·공유 유도.
- **공유 문구에 유형+지수 자동 포함** → "나는 인싸 캐럿 하우스 92점!" 식 바이럴 훅.
- 배치별 유형 분기·점수 범위·공유 문구·회귀(에러 0) 검증. (배치를 순수 함수로 판정 → 기존 이벤트 엔진 재사용, 리스크 낮음.)

## 15차 — 노잼 팝업 퇴치 (얼굴 없는 이벤트 제거 · 피날레 빌드업)
- **멤버리스 팝업 제거**: 얼굴 없이 📰 아이콘만 뜨던 `gose`(예능)·`position`(층) 이벤트에 **실제 입주 멤버 사진을 주입**. 예능 이벤트는 그날 입주한 멤버 3명이 '출연진'으로, 위치 이벤트는 해당 층 실입주자가 등장 → 모든 팝업에 얼굴이 뜸.
- **팝업 순서 빌드업 재정렬**: 표시 순서를 **얼굴 적은 것 → 많은 것** 순으로 정렬해, 마지막(5/5) 팝업이 **항상 얼굴 제일 많은 조합 이벤트**로 끝나도록. (기존엔 티어순 고정이라 허전한 이벤트가 피날레에 떨어짐.)
- **오프브랜드 예능 카피 교체**: 세븐틴st가 아닌 '돈치기 대회'·'관리비 체육대회' → **마피아 게임·릴레이 댄스 챌린지**로 리라이트.
- 회귀(랜덤·자동저장/복원·초기화·이사하기·스로틀·허니팟) + 팝업 5개 전부 얼굴 노출·피날레 최다 얼굴·에러 0 검증.

## 14차 — 팝업 안정화 · 멤버 사진 모핑 · 완전체 제거
- **팝업이 종종 안 뜨던 오류 수정**: 순차 팝업이 모션 팝업의 종료 콜백에 의존하던 구조를 제거하고, 이사하기 직후 **`openEventSequence()`를 직접 호출**하도록 변경. 모션 조건과 무관하게 항상 팝업이 뜸.
- **2인 이상 이벤트 멤버 사진 모핑**: 각 이벤트에 관련 멤버(`members`)를 실어, 팝업에 **해당 멤버 사진이 좌우에서 날아와 겹치는(clash) 연출**로 노출. 1인/게스트 이벤트는 단독/📰 아이콘.
- **완전체(13인) 입주 이벤트 완전 제거**: `combos`에서 완전체 항목 삭제 → 슬롯을 차지한 뒤 필터링돼 팝업이 4개만 뜨던 문제 해결. **이벤트 5개가 모두 노출**됨.
- 회귀(랜덤·자동저장·복원·초기화·이사하기·스로틀·허니팟) + 순차 팝업 5개 노출·멤버 모핑·에러 0 검증.

## 13차 — 이벤트 헤드라인화 & 순차 팝업
- **이벤트 문구 전면 리라이트**: 모든 `text`를 기사 헤드라인(후킹) 스타일로 (`[속보]`·`[단독]`·`[포착]`·`[목격담]` 등), `desc`는 한 줄 상황/대사.
- **순차 이벤트 팝업**: 이사하기 후 발생 이벤트를 **"📰 아파트 특보" 팝업으로 하나씩** 노출. `다음 →` 버튼으로 진행, 마지막은 `닫기`. **완전체(legendary) 입주는 제외**.
- 모션 팝업이 있으면 모션 종료 후 순차 팝업이 이어짐.
- 데이터는 `data/events.js`(관리자 편집). 파싱·순차 진행·완전체 제외·회귀 검증(에러 0).

## 12차 — 공유 이미지 수정 · 하단 공유바 · 공실 게스트 · 조건별 모션
- **공유 이미지 사진 미표시 버그 수정**: 캡처 전 아바타 이미지 **프리로드** + 캡처 중 애니메이션 정지(`.capturing`)로 반투명/미로딩 방지. `useCORS`·`imageTimeout` 추가.
- **저장·공유 버튼을 결과 카드 하단(`#shareBar`)으로 이동** — 비율 토글 + 저장 + 🐦/💬/📸 3버튼.
- **공실 게스트 카메오(`guests`)**: 지정 멤버 배치 + 공실이 있으면 세븐틴과 친한 인물이 놀러오는 이벤트(97즈·플레디스·크루 등, 공개 친분 기반·편집 가능).
- **조건별 모션 팝업(`motions`)**: 특정 배치에서 두 멤버 사진이 좌우에서 날아와 **충돌+플래시** 후 메시지 노출. 예) 캐럿 양옆이 도겸·승관 → "부석순 텐션 충돌!". `around+pair`(양옆) / `adjacent`(인접) 조건.
- 데이터는 `data/events.js`에 `guests`/`motions`로 분리(관리자 편집). 게스트 콘텐츠는 해외 포함 웹 리서치(97-line 등) 기반.

## 11차 — 플랫폼별 공유 포맷 (트위터·카카오톡·인스타 스토리)
- 단일 공유 버튼을 **🐦 트위터 · 💬 카카오톡 · 📸 인스타 스토리** 3버튼으로 분리.
- 각 공유 문구에 **플랫폼별 해시태그 + 결과 이벤트 중 랜덤 1개 + 닉네임 타이틀 + 서비스 URL** 자동 포함.
- 인스타는 **9:16 스토리 이미지**로 렌더. 모바일은 OS 공유 시트에서 앱 선택, 데스크톱은 트위터 작성창 열기 / 문구 복사 + 이미지 저장 폴백.
- 문구 생성·버튼 노출·9:16(0.562) 비율·회귀 검증(에러 0). README에 서비스 접속 링크 명시.

## 10차 — 데이터 외부화 + 방향성 케미 (관리자 편집 구조)
- **이벤트 데이터를 외부 파일 `data/events.js`로 분리** → 관리자가 GitHub에서 직접 편집(가이드 `data/README.md`). `index.html`은 `window.SVT_EVENT_DATA` 참조. 전역 할당 방식이라 `file://`·Pages 모두 동작, 미로드 시 안전 degrade.
- **방향성 케미(`directional`) 추가** — 왼쪽/오른쪽(같은 층)·위층/아래층(같은 라인) 구도. 예: "왼쪽 잠 요정 정한, 오른쪽 밤샘 우지", "위층 안무 쿵쿵 / 아래층 평온". 10종.
- 조합(`combos`) 2~13인으로 일반화, 위치(`position`)·고세를 데이터 선언형으로. **총 118개** 이벤트.
- 방향 판정(정/역방향)·데이터 로드·결정성·상한·회귀 전부 검증.

## 9차 — 이벤트 100+개 확장 & 상황/대사 추가 (소설·예능작가 페르소나)
- 페르소나에 **소설작가·예능작가** 추가. 회의 결과 이벤트 스키마를 `{ text, desc }`로 확장.
- **이벤트 총 107개**로 확장 (개인 53·케미 28·유닛 11·위치 4·고세 10 + 레전드 1).
- 각 이벤트에 **짧은 상황 묘사/대사** 부여, 결과 카드에 `제목 + 상황` 2줄로 표시(레전드 골드 강조).
- **멤버 성향 학습표**를 `docs/EVENT_ENGINE.md`에 문서화 — 서술 기준으로 반영.
- 개인 이벤트는 멤버당 다중 변형 + 시드 선택으로 리롤 다양성 강화. 결정성·상한·회귀 검증.

## 8차 — 나무위키 「멤버 간 케미」 자료 반영
[나무위키 세븐틴/멤버 간 케미](https://namu.wiki/w/세븐틴/멤버%20간%20케미) 문서(웹 검색으로 확인)의 팬덤 공인 케미명을 이벤트 데이터에 추가:
- 이웃 케미: **뱅쇼**(조슈아·승관) · **느모**(원우·버논) · **진짜꽃즈**(우지·도겸) · **도겸·민규 앙숙케미** · **호시·원우**(겁 많은 근육콤비)
- 조합 이벤트: **96즈/마불지즈**(준·호시·원우·우지) · **백구**(에스쿱스·정한·민규)
- 격리/상한 포함 발동 검증, 회귀 통과. 자료 출처는 `docs/EVENT_ENGINE.md`에 정리.

## 7차 — 이벤트 엔진 고도화 (6인 페르소나 회의 + 자료 수집)
정적 규칙 테이블을 **카테고리·티어·시드 기반 엔진**으로 확장 (`docs/EVENT_ENGINE.md` 참고).
- **6인 페르소나 회의**(트위터 고인물·시나리오 작가·서비스 기획자·디자이너·서버 개발자·아이돌 엔터사 기획자) 결론 반영.
- **카테고리**: 레전드 / 유닛·조합 / 고세 예능 / 이웃 케미 / 위치 / 개인. **티어 우선순위 + 카테고리별 상한**으로 다양성 확보(최대 5개).
- **레전드 이벤트**(13명 완전체·3개 유닛 완성) 골드 강조.
- **결정적 시드**(mulberry32): 같은 배치=같은 이야기, 다른 배치=다른 이야기. `generateEvents`는 순수 함수.
- **콘텐츠 자료 수집**: 웹/네이버 검색으로 유닛·부석순·믹스유닛 듀엣곡(동갑내기·어젯밤·Pretty Woman·Blue)·고잉세븐틴 포맷(TTT·돈치기·방탈출·어몽어스)·멤버 롤을 확인해 반영. (유튜브 API는 쿼터 초과로 미사용)

## 6차 — 닉네임 개인화 & 4:5 공유 변형
5차에서 보류했던 두 항목 적용:
- **닉네임 개인화** — 시작 단계에 닉네임 입력(localStorage 저장). 결과 카드 타이틀이 **"OO이 세븐틴 아파트에 입주했다"**로 변경. 한글 주격 조사(이/가) 자동 판별.
- **4:5 공유 변형** — 결과 카드에 "이미지 비율: 원본 / 4:5 공유용" 토글. 4:5는 컴팩트 렌더 후 **정확히 4:5(0.800) 캔버스에 크롭 없이 레터박스 합성** → 트위터/인스타 크롭 대응. 실물 렌더로 비율·내용 검증(1854×2318, ratio 0.800).

## 5차 — 결과 카드 구성 고도화 (D 서비스기획 · E 팬덤디자이너 페르소나)
결과 카드(공유 산출물) 구성을 두 신규 페르소나로 평가·토의 후 교집합 적용:
- **입주/공실 요약 지표 칩** — 헤더에 "💎 입주 N세대 · 🚪 공실 M세대 · 나도 입주/관전" (정보 위계).
- **캐럿(나) 칸 강조** — 내 칸에 로즈 링 + "나" 배지 (개인화·감정이입).
- **브랜드 워드마크** — 카드 하단에 `💎 SVT MANSION` (유입 단서·소장각).
- 보류(별도 확인): 트위터 크롭용 컴팩트 4:5 변형, 닉네임 개인화.

## 4차 — 멤버 성격 & 위치 기반 이웃 이벤트 (A 강화)
- 초기 A 진단의 미해결 결함("결과에 떡밥이 없음", "이웃 개념 미활용")을 정면으로 보강.
- **멤버 성격 데이터**(이모지 + 특성 태그) — 결과 카드 이름에 성격 이모지 배지 표시.
- **위치 기반 이웃 이벤트** — 같은 층 좌우 + 위아래 라인 인접을 분석해 케미 이벤트 생성.
  - 이웃 케미(특정 조합), 개인 특성(이웃 있으면 발동), 그룹 전원 배치(부석순·유닛) 3종. 우선순위 그룹>케미>개인, 최대 5개.
- 결과 카드에 **"🎬 우리 아파트 이야기"** 섹션 추가 → 저장/공유 이미지에도 포함(공유 떡밥). 실물 html2canvas 렌더로 검증.

## 3차 — 공유 이미지 버그 수정 & 로컬 벤더링
- **저장 이미지 해시태그 푸터 텍스트 누락 버그 수정** (`0792e40`)
  - html2canvas 1.4.1이 그라디언트 글자(`background-clip:text` + `color:transparent`)를 렌더하지 못해, 저장된 이미지에서 `#세븐틴아파트 · #SVT_Mansion`가 보이지 않던 문제.
  - → 브랜드 라일락(`#cdb7e0`) 솔리드 컬러로 교체. **실물 html2canvas 렌더**로 재현·수정을 검증.
- **html2canvas 로컬 벤더링** (`e5c122d`)
  - `vendor/html2canvas.min.js`(공식 1.4.1) 포함, 로딩을 **로컬 우선 → jsdelivr → unpkg** 폴백으로 전환. 오프라인 동작·버전 고정 확보.

## 2차 — 밝기 · 백엔드 보안 · 벤더링 방향
- **테마 밝기 상향** (`fbff6a7`) — 색상값을 CSS 변수로 토큰화하고 한 단계 밝은 blue-charcoal 다크로 조정 + 상단 세레니티 글로우. 기능 불변.
- **결과 전송 하드닝** (`a2a33e2`)
  - 서버: `apps_script/Code.gs` — 스키마 검증(15칸/floor·unit 범위/허용 입주자/좌표·멤버 중복 거부), 본문 크기 상한, 허니팟(`hp`) 무시, 선택적 토큰.
  - 클라이언트: 숨은 폼 허니팟 필드 + **5초 재전송 스로틀**.
- **벤더링 방향 문서화** (`9d80b1c`) — `vendor/README.md` + index.html 전환 스니펫(주석).

## C — 비주얼 리디자인
- **아파트 메타포 + SEVENTEEN 아이덴티티** (`8865284`)
  - 옥상 사인·층 태그·창문형 유닛·1F 출입구.
  - Rose Quartz·Serenity 상징색 그라디언트(타이틀/이사하기 버튼/층 라벨).
  - 선택·미리보기 아바타를 라운드 사각으로 통일 + 배치 팝 애니메이션 + 유닛 hover.
  - 저장 이미지에 옥상 배너·날짜·해시태그 푸터. 💎 이모지 파비콘.

## B — 기술·안정성
- **안정성 개선** (`9fca492`)
  - 배치 상태 localStorage 자동 저장/복원 + 전체 초기화 버튼.
  - Google Sheets 전송 상태 토스트(iframe onload + 타임아웃 폴백).
  - html2canvas jsdelivr→unpkg 이중 CDN 폴백 + 런타임 가드.
  - 아바타 이미지 로드 실패 폴백, 접근성(aria-label·`:focus-visible`).
  - '이대로 이사할까요?' 확인 후 미리보기 생성하도록 흐름 정리, README 보강.

## A — 재미·공유
- **랜덤 배치 · 원탭 공유 · 브랜딩** (`66e831b`)
  - 🎲 랜덤 배치(멤버 13 + 캐럿을 무작위 14칸, 1칸 공실).
  - 📲 Web Share API 원탭 공유 + 저장/클립보드 폴백.
  - 공유 이미지 해시태그 푸터 브랜딩.

---

> 개선 전 원본은 정적 배치 그리드 + 이미지 저장 + Google Sheets 전송 기능만 있었습니다. 위 순서로 **공유 루프 → 안정성 → 비주얼 → 백엔드 보안**을 차례로 보강했습니다.
