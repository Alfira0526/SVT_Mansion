# 이벤트 엔진 설계 (Event Engine)

결과 카드의 "🎬 우리 아파트 이야기"를 생성하는 엔진. 배치(위치·이웃·조합)를 읽어 **밝고 긍정적인 팬덤 서사 이벤트**를 만든다. 구현: `index.html`의 `generateEvents()` (순수·결정적 함수) + 규칙 데이터 테이블.

## 6인 페르소나 회의 결론 (설계 반영)
| 페르소나 | 요구 | 엔진 반영 |
|---|---|---|
| 트위터 고인물 | 짤/인용각, 리롤마다 새 이야기, '플렉스'용 레전드 | 배치별 결정적 시드(같은 배치=같은 이야기, 다른 배치=다른 이야기) + **레전드 이벤트**(골드 강조) |
| 시나리오 작가 | 톤 일관성, 카테고리 다양성, 반복 방지 | 카테고리(유닛/케미/위치/개인/고세) + **카테고리별 상한**으로 한 종류 도배 방지 |
| 서비스 기획자 | 명확한 규칙·우선순위·상한, 신뢰성 | **티어 우선순위**(레전드>유닛>고세>케미>위치>개인), 최대 5개 |
| 디자이너 | 카드에 들어갈 분량, 레전드 시각 구분 | 5줄 상한, 레전드는 `.legend`(골드) 스타일 |
| 서버 개발자 | 순수/결정적/테스트가능, 데이터·로직 분리, 조합폭발 방지 | `generateEvents(results)` 순수함수, 규칙은 데이터 테이블, mulberry32 시드 PRNG(Math.random 미사용) |
| 아이돌 엔터사 기획자 | 브랜드 세이프, 공개 사실 기반, 이미지 보호 | 공개 유닛/서브유닛/듀엣곡/자체예능 사실만 사용, 부정·사생활 언급 배제, 애정 어린 톤 |

## 이벤트 카테고리 (티어 순)
1. **레전드**(tier 0): 13명 완전체, 3개 유닛 전부 완성 → 골드 강조, 상한 무시.
2. **유닛/조합**(1): 힙합팀·보컬팀·퍼포먼스팀·부석순(BSS)·동갑내기(호시·우지)·막내즈(버논·디노) 전원 배치 시.
3. **고세 예능**(2): 6명 이상 배치 시 고잉세븐틴 포맷 이벤트 1개(방탈출/돈치기/어몽어스/TTT/노래방).
4. **이웃 케미**(3): 같은 층 좌우 + 위아래 라인 인접 시. 실제 믹스유닛 듀엣곡 레퍼런스 포함.
5. **위치**(4): 5층(펜트하우스)·1층(로비) 배치.
6. **개인**(5): 이웃이 있으면 멤버 캐릭터 이벤트(변형 중 시드로 하나).

## 선택 로직
- 발동한 이벤트를 모아 시드 셔플 → 티어 오름차순 안정 정렬 → 카테고리 상한(유닛3/케미2/위치1/개인2/고세1) 적용 → 최대 5개.
- 시드는 배치(칸-멤버) 해시 기반 → **같은 배치는 항상 같은 이야기**(신뢰성), 배치가 바뀌면 이야기도 바뀜(리롤 재미).

## 콘텐츠 자료 출처 (수집)
공개 자료로 유닛 구성·서브유닛·믹스유닛 듀엣·자체예능 포맷을 확인해 문구에 반영:
- **유닛 구성 / 부석순 / 믹스유닛**: 나무위키·위키백과·Weverse Magazine, 언론(이투데이 등) — 힙합팀(에스쿱스·원우·민규·버논), 보컬팀(정한·조슈아·우지·도겸·승관), 퍼포먼스팀(준·호시·디에잇·디노), 부석순(승관·도겸·호시).
- **믹스유닛 듀엣곡**: 호시×우지 '동갑내기', 정한×원우 '어젯밤', 에스쿱스×민규 'Pretty Woman', 도겸×승관 'Blue'.
- **고잉세븐틴(자체예능) 포맷**: 팬 블로그 다수 — TTT(MT/썰), 돈치기/돈't Lie, 방탈출, 어몽어스 현실판, 노래방 등.
- **멤버 롤/별명**: Weverse Magazine·팬 정리 — 리더(에스쿱스), 프로듀서·천재작곡가(우지), 텐션·호랑이(호시), 예능·리액션(승관), deadpan·게임(원우), 살림·사고(민규) 등.

> 유튜브 Data API는 수집 시점에 일일 쿼터 초과로 미사용. 웹/네이버 검색으로 대체 수집.

## 확장 방법
`UNIT_EVENTS` / `PAIR_EVENTS` / `SOLO_EVENTS` / `POSITION_EVENTS` / `GOSE_EVENTS` 데이터 배열에 항목만 추가하면 된다. 로직(`generateEvents`)은 수정 불필요.
